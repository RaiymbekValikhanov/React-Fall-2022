import { Container, Item, Segment, Button, Label, Icon, Grid } from "semantic-ui-react";
const Rules = () => {
    return (
        <Container>
            <Grid>
                <Grid.Row>
                    
                </Grid.Row>
                <Grid.Row>

                </Grid.Row>
            </Grid>
        </Container>
    )
}

export default Rules;